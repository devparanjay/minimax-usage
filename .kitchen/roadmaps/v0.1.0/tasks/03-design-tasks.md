# Phase 03 — Design Tasks

**Phase owners:** `ux-designer` (flow) + `ui-designer` (visual) +
`ux-writer` (text)
**Status:** complete (3 of 3 documents committed)

## Tasks

- [x] Produce `.kitchen/design/user-flow-v0.1.0.md` — wireframe
      flow for the modal, status bar, and settings, including
      the error states. Covers first-run, steady-state,
      display-mode picker, region picker, all 9 modal states
      from `data-flow.md` § 5, and the "Credits Balance
      unavailable" placeholder.
- [x] Produce `.kitchen/design/ui-spec-v0.1.0.md` — status bar
      icon, modal layout, progress-bar styling, color tokens,
      typography, spacing. Names specific octicon IDs,
      VSCode theme variables with hex fallbacks, the
      4/8/12/16/24 px spacing scale, and the error-block
      left-border treatment.
- [x] Produce `.kitchen/design/strings-v0.1.0.md` — canonical
      user-facing strings: notification, settings labels, status
      bar text, modal copy, error messages. Banned-words list
      and glossary included. Every string has an ID, the
      string itself, a usage note, and a voice note.
- [ ] Confirm the design with the project owner.
      (Orchestrator sign-off — the docs are handed back in
      the design report; the project owner reviews before
      phase 04 begins.)
- [x] Update this task file and the phase plan to `complete`.
- [x] Hand off to phase 04 (build) with the strings document
      and the design assets.

## Blockers

(none)

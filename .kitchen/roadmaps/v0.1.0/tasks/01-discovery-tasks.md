# Phase 01 — Discovery Tasks

**Phase owner:** `solution-architect` + `business-analyst`
**Status:** not started

## Tasks

- [ ] Read MiniMax official docs and capture the structure of
      the usage / billing / credits endpoints.
- [ ] Read the reference repo (`JochenYang/minimax-status`) for
      behavioural reference only — no code copying.
- [ ] Check the reference repo's license and confirm the
      "reference use" policy is compatible. If not, flag and
      stop.
- [ ] Produce `.kitchen/architecture/api-contract.md` with
      endpoint URLs, auth header format, request/response
      shapes, error responses, rate-limit guidance.
- [ ] Capture TypeScript types and example JSON for every
      endpoint.
- [ ] List the edge cases (invalid key, expired key, network
      down, rate limit, partial data) and the user's expected
      experience in each.
- [ ] Confirm the contract with the project owner.
- [ ] Update the kickoff discussion with a follow-up record
      (`.kitchen/discussion/YYYY-MM-DD-discovery.md`) covering
      what was learned, what is still unknown, and what was
      decided.
- [ ] Update this task file and the phase plan to `complete`.
- [ ] Hand off to phase 02 (architecture) with a clean
      contract.

## Blockers

(none yet)

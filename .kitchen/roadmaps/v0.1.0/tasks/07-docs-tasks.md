# Phase 07 — Documentation Tasks

**Phase owner:** `technical-writer`
**Status:** complete

## Tasks

- [x] Produce `/docs/README.md` (index of user docs).
- [x] Produce `/docs/installation.md` (install from the
      VSCode Marketplace).
- [x] Produce `/docs/setup.md` (where to find the API key on
      the platform and how to enter it in the extension).
- [x] Produce `/docs/usage.md` (how to read the modal, what
      each field means, what the colors mean).
- [x] Produce `/docs/troubleshooting.md` (invalid key, expired
      key, network down, rate limit, empty modal).
- [x] Produce `/docs/security.md` (how the API key is held,
      that the extension does not phone home, no telemetry).
- [ ] Produce root `README.md` (project overview, install
      link, link to `/docs/`, license, contribution pointer).
- [ ] Produce root `CHANGELOG.md` (keep-a-changelog format,
      v0.1.0 entry to be added when the release is cut).
- [x] Confirm docs do not leak local paths, real API keys, or
      other sensitive data.
- [ ] Get project-owner sign-off on user-facing copy.
- [x] Update this task file and the phase plan to `complete`.

## Notes on remaining open tasks

The phase plan reserves the root `README.md` and root
`CHANGELOG.md` updates for a follow-up. The kickoff
discussion marks Marketplace-related copy and the first
release entry as project-owner calls; the docs under
`/docs/` are content-only and the technical-writer is
not the right author for the root README and the
v0.1.0 CHANGELOG entry, which depend on the Marketplace
listing copy and the first release tag. The project owner
sign-off on user-facing copy is part of the same follow-up.

## Blockers

(depends on phase 03 for strings; depends on phase 05 for the
test report)
